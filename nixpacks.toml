[phases.setup]
# Use a modern Node version compatible with Vite 6.
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["npm install", "npm install -g serve"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "serve -s dist -l ${PORT:-3000} --no-clipboard --single"
